import React from 'react';
import HomeView from './home.view';

class Home extends React.Component {
constructor(props) {
    super(props);

    this.state = {
    };
  }

  render() {
    return (
      <HomeView/>
    );
  }
}

export default Home;
